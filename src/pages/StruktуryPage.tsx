import { Navbar } from "@/components/Navbar";
import { ArtDecoDivider } from "@/components/ArtDecoDivider";
import { SectionCard, SectionTopic } from "@/components/SectionCard";
import { ListChart, DictChart, SetTupleChart, ListComprehensionChart } from "@/components/SectionCharts";

const topics: SectionTopic[] = [
  {
    title: "Списки (list)",
    icon: "List",
    intro: "Список — упорядоченная изменяемая коллекция элементов любых типов. Это самая часто используемая структура данных в Python. Элементы хранятся в определённом порядке, каждый имеет числовой индекс, начинающийся с нуля.",
    steps: [
      { label: "Создание", text: "Список создаётся в квадратных скобках: fruits = [\"яблоко\", \"банан\", \"вишня\"]. Элементы могут быть любого типа — строки, числа, другие списки." },
      { label: "Индексация", text: "Каждый элемент имеет номер (индекс), начиная с 0. fruits[0] — первый элемент, fruits[-1] — последний. Попытка обратиться к несуществующему индексу вызовет ошибку IndexError." },
      { label: "Методы", text: "append() добавляет элемент в конец, remove() удаляет первое вхождение, len() возвращает длину, sort() сортирует. Всё это встроено — ничего устанавливать не нужно." },
    ],
    visual: <ListChart />,
    blocks: [
      {
        label: "Код:",
        code: `fruits = ["яблоко", "банан", "вишня"]

fruits.append("груша")      # добавить в конец
fruits.remove("банан")      # удалить элемент
print(fruits[0])            # яблоко — первый элемент
print(len(fruits))          # 3 — длина списка`,
      },
      { label: "Результат:", code: `яблоко\n3` },
    ],
  },
  {
    title: "Словари (dict)",
    icon: "BookOpen",
    intro: "Словарь хранит пары «ключ: значение». Это аналог JSON в Python — идеальная структура для хранения структурированных данных о сущностях: пользователях, товарах, настройках. Ключ всегда уникален.",
    steps: [
      { label: "Создание", text: "Словарь создаётся в фигурных скобках: {\"имя\": \"Иван\", \"возраст\": 30}. Ключами могут быть строки и числа. Значениями — что угодно." },
      { label: "Доступ", text: "Значение читается по ключу: person[\"имя\"]. Если ключа нет — ошибка KeyError. Безопасный вариант: person.get(\"ключ\", \"значение по умолчанию\")." },
      { label: "Методы", text: "keys() — все ключи, values() — все значения, items() — все пары. Новый ключ добавляется присваиванием: person[\"email\"] = \"a@b.ru\"." },
    ],
    visual: <DictChart />,
    blocks: [
      {
        label: "Код:",
        code: `person = {
    "имя": "Иван",
    "возраст": 30,
    "город": "Москва"
}

print(person["имя"])        # Иван
person["email"] = "i@i.ru" # добавить ключ
print(person.keys())        # все ключи`,
      },
      { label: "Результат:", code: `Иван\ndict_keys(['имя', 'возраст', 'город', 'email'])` },
    ],
  },
  {
    title: "Кортежи и множества",
    icon: "Layers",
    intro: "Кортеж (tuple) — неизменяемый список. Множество (set) — коллекция уникальных значений без порядка. Выбор между ними зависит от задачи: кортеж — для фиксированных данных, множество — для уникальности и теоретико-множественных операций.",
    steps: [
      { label: "Кортеж", text: "Создаётся в круглых скобках: (55.75, 37.62). После создания элементы нельзя изменить. Это гарантия неизменности — координаты, RGB-цвет, константы." },
      { label: "Множество", text: "Создаётся в фигурных скобках: {1, 2, 3}. Автоматически убирает дубликаты. Поддерживает математические операции: & (пересечение), | (объединение), - (разность)." },
    ],
    visual: <SetTupleChart />,
    blocks: [
      {
        label: "Множество — убираем дубликаты:",
        code: `tags = {"python", "код", "python", "обучение"}
print(tags)  # {'python', 'код', 'обучение'} — без дублей

a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)   # {2, 3} — пересечение
print(a | b)   # {1, 2, 3, 4} — объединение`,
      },
    ],
  },
  {
    title: "Перебор коллекций",
    icon: "Repeat",
    intro: "Все коллекции в Python поддерживают итерацию — перебор элементов циклом for. Для словарей есть удобные методы: .items() даёт пары ключ-значение, .keys() — только ключи, .values() — только значения.",
    steps: [
      { label: "for + список", text: "for item in my_list: — на каждой итерации переменная item принимает следующее значение из списка. Порядок гарантирован." },
      { label: "for + словарь", text: "for key, value in my_dict.items(): — одновременно получаем и ключ, и значение. Это самый удобный способ перебора словаря." },
      { label: "enumerate", text: "enumerate(my_list) даёт одновременно индекс и значение: for i, item in enumerate(fruits). Удобно когда нужен номер элемента." },
    ],
    blocks: [
      {
        label: "Перебор списка и словаря:",
        code: `# Перебор списка
for fruit in ["яблоко", "банан"]:
    print(fruit)

# Перебор словаря
user = {"имя": "Анна", "возраст": 25}
for key, value in user.items():
    print(f"{key}: {value}")`,
      },
      {
        label: "Результат:",
        code: `яблоко\nбанан\nимя: Анна\nвозраст: 25`,
      },
    ],
  },
  {
    title: "List comprehension",
    icon: "Zap",
    intro: "List comprehension — способ создать новый список одной строкой кода на основе существующей коллекции. Это не просто сокращение — это питоновский идиоматичный стиль, который делает код выразительным и понятным. Заменяет цикл for с append().",
    steps: [
      { label: "Базовый синтаксис", text: "[выражение for элемент in коллекция] — создаёт список, применяя выражение к каждому элементу." },
      { label: "С фильтром", text: "[выражение for элемент in коллекция if условие] — включает только те элементы, для которых условие True." },
      { label: "Когда применять", text: "Используйте comprehension для простых преобразований и фильтрации. Если логика сложная — лучше обычный цикл for для читаемости." },
    ],
    visual: <ListComprehensionChart />,
    blocks: [
      {
        label: "Примеры:",
        code: `numbers = [1, 2, 3, 4, 5]

# Квадраты чисел одной строкой
squares = [n ** 2 for n in numbers]
print(squares)   # [1, 4, 9, 16, 25]

# Только чётные
evens = [n for n in numbers if n % 2 == 0]
print(evens)     # [2, 4]`,
      },
    ],
  },
  {
    title: "Вложенные структуры",
    icon: "Database",
    intro: "Структуры данных можно вкладывать друг в друга. Список словарей — стандартный способ хранить коллекцию однотипных объектов (например, базу пользователей). Словарь словарей — для иерархических данных.",
    steps: [
      { label: "Список словарей", text: "users = [{\"имя\": \"Иван\"}, {\"имя\": \"Анна\"}] — типичная структура, возвращаемая из базы данных или API. Каждый словарь — одна запись." },
      { label: "Доступ", text: "users[0][\"имя\"] — берём первый элемент списка (словарь), затем читаем ключ. Можно перебирать циклом for." },
    ],
    blocks: [
      {
        label: "Список пользователей:",
        code: `users = [
    {"имя": "Иван", "возраст": 30},
    {"имя": "Анна", "возраст": 25},
    {"имя": "Олег", "возраст": 35},
]

# Имена всех пользователей
names = [u["имя"] for u in users]
print(names)   # ['Иван', 'Анна', 'Олег']

# Только старше 28
adults = [u for u in users if u["возраст"] > 28]
print(len(adults))   # 2`,
      },
      { label: "Результат:", code: `['Иван', 'Анна', 'Олег']\n2` },
    ],
  },
];

export default function StruktуryPage() {
  return (
    <div className="min-h-screen bg-background dark">
      <Navbar />
      <main className="pt-24 pb-24 px-6">
        <div className="max-w-4xl mx-auto">
          <div className="mb-12 text-center">
            <p className="text-primary tracking-[0.2em] uppercase text-sm mb-4">Раздел 2</p>
            <h1 className="font-serif text-4xl md:text-5xl text-foreground mb-4">Структуры данных</h1>
            <p className="text-muted-foreground text-lg max-w-xl mx-auto">
              Списки, словари, кортежи и множества — основные способы хранить и организовывать данные в Python.
            </p>
          </div>
          <ArtDecoDivider variant="stepped" />
          <div className="space-y-12 mt-12">
            {topics.map((topic) => <SectionCard key={topic.title} topic={topic} />)}
          </div>
        </div>
      </main>
    </div>
  );
}
